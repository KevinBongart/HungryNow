<div class="row">
  <ul class="media-grid">
    <% @recipes.each do |recipe| %>
      <li>
        <%= link_to image_tag(recipe.thumb, :class => 'thumbnail'), recipe.pf_url %>
      </li>
    <% end %>  
  </ul>  
</div>
